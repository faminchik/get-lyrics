import React from 'react';

const Spinner = () => {
    return (
        <div style={{ margin: '190px auto' }}>
            <i className="zmdi zmdi-refresh zmdi-hc-spin zmdi-hc-4x" />
        </div>
    );
};

export default Spinner;
